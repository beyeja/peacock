import React from 'react';

const PagePlayerContainer = ({ children }: { children: React.ReactNode }) => (
    <div className='self-stretch px-10 space-y-6'>{children}</div>
);
export default PagePlayerContainer;
